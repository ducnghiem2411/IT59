import { Request, Response, NextFunction } from 'express'

export async function userAuthorization(req: Request, res: Response, next: NextFunction): Promise<> {
    try {
        
    } catch (e) {
        return { code: 401 }
    }    
}